server {
    listen   ${NGINX_PORT};

    root /app;

    index    index.php;

    location / {
        # try to serve file directly, fallback to index.html
        try_files $uri /index.php$is_args$args;
    }

}